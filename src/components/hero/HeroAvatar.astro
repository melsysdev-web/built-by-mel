---
import { Image } from "astro:assets";
import profile from "../../assets/profilev1.webp";
---

<div class="relative group">
  <!-- Glow -->
  <div
    class="absolute -inset-2 rounded-full
           bg-primary/40 blur-lg
           opacity-0 transition duration-300
           group-hover:opacity-100
           z-0">
  </div>

  <!-- Avatar -->
  <div
    class="relative size-52 rounded-full
           overflow-hidden z-10">
    <Image
      src={profile}
      alt="Professional headshot of a systems engineer"
      width={600}
      height={600}
      class="w-full h-full object-cover object-[center_10%]"
    />
  </div>
</div>
